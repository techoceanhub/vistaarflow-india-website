document.addEventListener('DOMContentLoaded',()=>{const h=document.querySelector('.site-header'),b=document.querySelector('.nav-toggle'),n=document.querySelector('.primary-nav');window.addEventListener('scroll',()=>h&&h.classList.toggle('scrolled',scrollY>20),{passive:true});if(b&&n)b.addEventListener('click',()=>{const o=b.getAttribute('aria-expanded')==='true';b.setAttribute('aria-expanded',String(!o));n.classList.toggle('open')});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{n&&n.classList.remove('open');b&&b.setAttribute('aria-expanded','false')}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const dashboard=document.querySelector('.animated-dashboard'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(dashboard){const counters=[...dashboard.querySelectorAll('.dashboard-counter')],format=(el,value)=>el.closest('.metric-revenue')?(Number(value)/10).toFixed(1):Math.round(Number(value)).toLocaleString('en-IN'),finish=()=>counters.forEach(el=>el.textContent=format(el,el.dataset.count));if(reduced){finish()}else{const counterObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const start=performance.now(),duration=1500;const tick=now=>{const progress=Math.min((now-start)/duration,1),eased=1-Math.pow(1-progress,3);counters.forEach(el=>el.textContent=format(el,Number(el.dataset.count)*eased));if(progress<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);counterObserver.disconnect()}),{threshold:.45});counterObserver.observe(dashboard)}}});

document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.faq-list').forEach(list=>{const items=[...list.querySelectorAll('.faq-item')],numbers=list.querySelector('.faq-page-numbers'),prev=list.querySelector('.faq-page-prev'),next=list.querySelector('.faq-page-next'),pageSize=3,total=Math.ceil(items.length/pageSize);let page=0;if(!numbers||!prev||!next)return;const render=()=>{items.forEach((item,index)=>{item.hidden=Math.floor(index/pageSize)!==page;if(item.hidden)item.open=false});[...numbers.children].forEach((button,index)=>{if(index===page)button.setAttribute('aria-current','page');else button.removeAttribute('aria-current')});prev.disabled=page===0;next.disabled=page===total-1};for(let i=0;i<total;i++){const button=document.createElement('button');button.type='button';button.textContent=String(i+1);button.setAttribute('aria-label',`FAQ page ${i+1}`);button.addEventListener('click',()=>{page=i;render()});numbers.append(button)}prev.addEventListener('click',()=>{if(page>0){page--;render()}});next.addEventListener('click',()=>{if(page<total-1){page++;render()}});list.classList.add('faq-ready');render()})});

document.addEventListener('DOMContentLoaded',()=>{const themeUrl=document.querySelector('.hero-dashboard-image')?.src.replace(/\/assets\/images\/[^/]+(?:\?.*)?$/,'')||'';const pipeline=document.querySelector('.pipeline-image-wrap');if(pipeline&&themeUrl){pipeline.innerHTML=`<img class="pipeline-reference-image" src="${themeUrl}/assets/images/flexible-pipeline-premium-light.png" alt="Sales pipeline moving from New Lead through Contacted, Qualified and Proposal to Won, with automated reminders, notifications, email and workflow actions">${Array.from({length:10},(_,i)=>`<span class="pipeline-node-layer pipeline-node-${i+1}" aria-hidden="true"></span>`).join('')}`}const agent=document.querySelector('.success-agent');if(agent&&themeUrl){agent.removeAttribute('aria-hidden');agent.querySelector('svg')?.remove();const image=document.createElement('img');image.className='success-agent-image';image.src=`${themeUrl}/assets/images/with-you-support-vistaar.png`;image.alt='VistaarFlow customer success and account support';agent.querySelector('.agent-status')?.before(image)}const panel=document.querySelector('.success-support-panel');if(panel&&!panel.querySelector('.plan-notice')){panel.insertAdjacentHTML('beforeend','<aside class="plan-notice"><strong>Please Note</strong><ul><li>CRM credits cover AI features, workflow automation, WhatsApp Flows, campaigns, and platform usage.</li><li>Additional credits, storage, and premium add-ons can be purchased at any time.</li><li>Some integrations and features may vary depending on the selected subscription plan.</li></ul></aside>')}});

document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.faq-list').forEach(list=>{const items=[...list.querySelectorAll('details.faq-item')];items.forEach(item=>item.addEventListener('toggle',()=>{if(!item.open)return;items.forEach(other=>{if(other!==item)other.open=false})}))})});
